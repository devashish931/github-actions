name: build
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: pull code
        uses: actions/checkout@v6
      - name: install dependencies
        run: yarn install --immutable
      - name: create build
        run: yarn run build
